import { FlexPlugin } from 'flex-plugin';
import React from 'react';
import CustomNotificationElement from "./CustomNotificationElement";

export default class MiccheckPlugin extends FlexPlugin {
  name = 'MiccheckPlugin';

  init(flex, manager) {

  }
}
